# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
._*

# --- Node / 前端 ---
node_modules/
dist/
*.local
*.log
*.tsbuildinfo

# --- Rust / Cargo / Tauri ---
src-tauri/target/
# 本地 cargo 缓存（本仓库曾含 .cargo-home，勿提交）
.cargo-home/
# Tauri 打包产物（app / dmg 在 src-tauri/target 内，此处兜底）
release/
bundle/

# --- 编辑器/脚本临时备份 ---
*.bak
*~

# 注：DepotDownloader sidecar 二进制已用 Git LFS 纳入（见 .gitattributes），不再忽略。

# --- 本地运行数据（若误放仓库内）---
*.db
*.db-journal
*.db-wal
credentials.dat

# --- IDE / 编辑器 ---
.idea/
.vscode/
*.swp
*.swo
.pnpm-store/

.v2c/
.video_agent/
.zcode/
dist-linux/

# Linux 打包资源暂存（build.rs 按目标架构从 sdk/ 生成）
src-tauri/bundled/
