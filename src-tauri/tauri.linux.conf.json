{
  "$schema": "https://schema.tauri.app/config/2",
  "bundle": {
    "targets": [
      "deb",
      "appimage"
    ],
    "resources": {
      "../dist/renderer": "renderer",
      "../dist/assets": "assets",
      "bundled/libsteam_api.so": "libsteam_api.so"
    }
  }
}
